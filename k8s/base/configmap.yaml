# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: backend-app-config
# data:
#   APP_NAME: "backend-app"
#   DEBUG: "false"
#   API_V1_PREFIX: "/api/v1"


apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    ports:
    - containerPort: 80